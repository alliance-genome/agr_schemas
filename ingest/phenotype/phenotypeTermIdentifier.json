{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Global IDs",
  "id": "phenotypeTermIdentifier.json#",
  "description": "Group of ids that represent a phenotype statement, along with an order to combine for display eventually.",
  "type": "object",
  "required": ["termId","termOrder"],
  "additionalProperties": false,
  "properties": {
    "termId": {
    	    "$ref": "../globalId.json#/properties/globalId",
	    "description":"the id that represents a part, or the entire, phenotype statement - an ontology id."    
    },
    "termOrder": {
    	      "type":"integer"
    }
  }
}

{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "expressedComponent",
  "description": "Expressed region driving expression: sometimes there isn't an ID, so name is the pr\
imary key, with an optional id.",
  "id": "expressedComponent.json#",
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "primaryId": {
      "$ref": "../globalId.json#/properties/globalId",
    },
    "name": {
      "type": "string"
    }
}
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "variant",
  "description": "An entry with Variant information from a MOD.",
  "id": "variant.json#",
  "type": "object",
  "required": ["primaryId", "taxonId", "type","alleleId"],
  "additionalProperties": false,
  "properties": {
    "alleleId": {
      "$ref": "../globalId.json#/properties/globalId",
      "description": "The prefixed primary (MOD) ID for the variant."
    },
    "taxonId": {
      "$ref": "../globalId.json#/properties/globalId",
      "description" : "The taxonId for the species of the gene entity."
    },
    "variants": { 
      "type": "array",
     "items": {
     	      "$ref" : "variantDetail.json"
     },
      "description": "variant detail dictates recorded genomic location and sequence information, which is captured in a subschema for each type allowed."
    }
  }
 }
}
